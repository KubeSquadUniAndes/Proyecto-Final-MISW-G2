###################################
# DestinationRules with outlier detection
#
# Outlier detection is the IMMEDIATE response to 5xx errors:
# - When a pod returns 3 consecutive 5xx errors, it gets ejected
#   from the load balancing pool for 30s.
# - This forces traffic to healthy replicas instantly.
# - Combined with HPA, new replicas are created if load is sustained.
###################################
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: detector-anomalias-dr
  namespace: workloads
spec:
  host: detector-anomalias-service.workloads.svc.cluster.local
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 800
      http:
        h2UpgradePolicy: DEFAULT
        http1MaxPendingRequests: 500
        http2MaxRequests: 1000
        maxRequestsPerConnection: 10
        maxRetries: 3
    outlierDetection:
      consecutive5xxErrors: 3
      interval: 5s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: hospedajes-dr
  namespace: workloads
spec:
  host: hospedajes-service.workloads.svc.cluster.local
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 800
      http:
        h2UpgradePolicy: DEFAULT
        http1MaxPendingRequests: 500
        http2MaxRequests: 800
        maxRequestsPerConnection: 10
        maxRetries: 2
    outlierDetection:
      consecutive5xxErrors: 2
      interval: 5s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
  subsets:
  - name: v1
    labels:
      version: v1
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: login-handler-dr
  namespace: workloads
spec:
  host: login-handler-service.workloads.svc.cluster.local
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 800
      http:
        h2UpgradePolicy: DEFAULT
        http1MaxPendingRequests: 500
        http2MaxRequests: 1000
        maxRequestsPerConnection: 10
        maxRetries: 3
    outlierDetection:
      consecutive5xxErrors: 3
      interval: 5s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: notificaciones-dr
  namespace: workloads
spec:
  host: notificaciones-service.workloads.svc.cluster.local
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 800
      http:
        h2UpgradePolicy: DEFAULT
        http1MaxPendingRequests: 500
        http2MaxRequests: 1000
        maxRequestsPerConnection: 10
        maxRetries: 3
    outlierDetection:
      consecutive5xxErrors: 3
      interval: 5s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: reservas-dr 
  namespace: workloads
spec:
  host: reservas-service.workloads.svc.cluster.local
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 800
      http:
        h2UpgradePolicy: DEFAULT
        http1MaxPendingRequests: 500
        http2MaxRequests: 1000
        maxRequestsPerConnection: 10
        maxRetries: 3
    outlierDetection:
      consecutive5xxErrors: 3
      interval: 5s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: users-dr
  namespace: workloads
spec:
  host: users-service.workloads.svc.cluster.local
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 800
      http:
        h2UpgradePolicy: DEFAULT
        http1MaxPendingRequests: 500
        http2MaxRequests: 800
        maxRequestsPerConnection: 10
        maxRetries: 3
    outlierDetection:
      consecutive5xxErrors: 3
      interval: 5s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
  subsets:
  - name: v1
    labels:
      version: v1
