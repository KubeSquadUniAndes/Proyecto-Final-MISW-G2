apiVersion: v1
kind: Service
metadata:
  name: reservations-service
  namespace: travelhub
spec:
  selector:
    app: reservations-service
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
